<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>로그인 완료</title>
  <style>
    body {
      margin: 0; padding: 0;
      background: #0a0f1e;
      display: flex; align-items: center; justify-content: center;
      min-height: 100vh;
      font-family: -apple-system, BlinkMacSystemFont, sans-serif;
      color: white;
    }
    .container { text-align: center; padding: 24px; }
    .spinner {
      width: 40px; height: 40px;
      border: 3px solid rgba(255,255,255,0.15);
      border-top: 3px solid #2563eb;
      border-radius: 50%;
      animation: spin 0.8s linear infinite;
      margin: 0 auto 20px;
    }
    @keyframes spin { to { transform: rotate(360deg); } }
    h2 { font-size: 18px; margin-bottom: 8px; }
    p { font-size: 14px; color: rgba(255,255,255,0.5); }
    .btn {
      display: inline-block; margin-top: 20px;
      padding: 12px 24px; background: #2563eb;
      color: white; border-radius: 12px;
      text-decoration: none; font-weight: 700; font-size: 14px;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="spinner"></div>
    <h2>✅ Google 로그인 성공!</h2>
    <p>앱으로 돌아가는 중...</p>
    <a class="btn" href="<%= @deep_link %>" id="return-btn" style="display:none;">앱으로 돌아가기</a>
    <a class="btn" href="<%= @web_fallback || "/auth/token_login?token=#{@deep_link.split('token=').last}" %>" id="web-btn" style="display:none; margin-top:10px; background:#374151;">웹에서 계속하기</a>
  </div>
  <script>
    var deepLink = '<%= @deep_link %>';
    var token = deepLink.split('token=')[1];
    var webFallback = window.location.origin + '/auth/token_login?token=' + token;

    // iOS 감지
    var isIOS = /iPhone|iPad|iPod/i.test(navigator.userAgent);

    if (isIOS) {
      // iOS: WebView가 폴링으로 토큰을 받아서 자동 처리함
      // SFSafariViewController 안에서는 이동하지 않음 (쿠키 비공유 문제)
      // 딥링크도 시도 (혹시 작동하면 더 빠름)
      var iframe = document.createElement('iframe');
      iframe.style.display = 'none';
      iframe.src = deepLink;
      document.body.appendChild(iframe);

      // 메시지 업데이트: 자동 처리됨을 안내
      setTimeout(function() {
        document.querySelector('p').textContent = '잠시만 기다려주세요...';
      }, 2000);

      // 5초 후에도 안 닫혔으면 수동 안내
      setTimeout(function() {
        document.querySelector('p').textContent = '왼쪽 상단 X를 눌러 앱으로 돌아가세요';
        document.querySelector('p').style.color = '#60a5fa';
        document.querySelector('p').style.fontWeight = '700';
      }, 5000);
    } else {
      // Android: 기존 방식 - 직접 딥링크
      window.location.href = deepLink;

      // 2초 후에도 남아있으면 수동 버튼 표시
      setTimeout(function() {
        document.getElementById('return-btn').style.display = 'inline-block';
      }, 2000);
    }
  </script>
</body>
</html>
